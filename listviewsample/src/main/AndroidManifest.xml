<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.xxt.listviewsample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ArrayAdapterActivity" />
        <activity android:name=".MyListActivity" />
        <activity android:name=".CustomItemActivity" />
        <activity android:name=".CustomArrayAdapterActivity" />
        <activity android:name=".SimpleAdapterActivity"></activity>
        <activity android:name=".CustomBaseAdapterActivity">

        </activity>
        <activity android:name=".UpdataListViewActivity">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>