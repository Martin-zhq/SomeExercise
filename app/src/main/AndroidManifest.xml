<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.xxt.recyclerviewexercise">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".transmitdata.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".recyclerviewexercise.ShowDateTime"
            android:label="@string/date_and_time" />
        <activity android:name=".transmitdata.TransmitDataActivity" />
        <activity android:name=".transmitdata.MyActivity1" />
        <activity android:name=".transmitdata.MyActivity2" />
        <activity android:name=".transmitdata.MyActivity3" />
        <activity android:name=".transmitdata.MyActivity4" />
        <activity android:name=".startactivityforresult.ResultActivity" />
        <activity android:name=".startactivityforresult.RequestActivity" />
        <activity android:name=".systemactivity.SystemActivity" />
        <activity android:name=".invokeotheractivity.InvokeOtherActivity" />
        <activity android:name=".filterconsumer.FilterConsumerActivity" />
        <activity
            android:name=".screenorientation.ScreenOrientationActivity"
            android:screenOrientation="nosensor" />
        <activity
            android:name=".configchanges.ConfigChangesActivity"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="fullSensor" />
        <activity android:name=".process.ProcessActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".process.AActivity"
            android:process=":new_process" />
        <activity android:name=".process.BActivity"></activity>
    </application>

</manifest>